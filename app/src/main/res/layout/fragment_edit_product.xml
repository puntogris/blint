<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.puntogris.blint.ui.product.create_edit.EditProductViewModel" />

        <variable
            name="fragment"
            type="com.puntogris.blint.ui.product.create_edit.EditProductFragment" />
    </data>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="?actionBarSize"
            android:orientation="vertical">

            <include
                android:id="@+id/descriptionLayout"
                layout="@layout/product_description_expandable"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                app:fragment="@{fragment}"
                app:viewModel="@{viewModel}" />

            <include
                android:id="@+id/pricesLayout"
                layout="@layout/product_prices_expandable"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                app:fragment="@{fragment}"
                app:viewModel="@{viewModel}" />

            <include
                android:id="@+id/scopeLayout"
                layout="@layout/product_scope_expandable"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                app:fragment="@{fragment}"
                app:viewModel="@{viewModel}" />

            <include
                android:id="@+id/productExtrasLayout"
                layout="@layout/product_extras_expandable"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="20dp"
                app:fragment="@{fragment}"
                app:viewModel="@{viewModel}" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>
</layout>